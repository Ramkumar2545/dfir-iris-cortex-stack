# Runtime dependencies installable from PyPI.
# iris-module-interface is NOT listed here — it is pre-installed by
# the IRIS container image and is not available on PyPI.
requests>=2.28
jinja2>=3.0
urllib3>=1.26
